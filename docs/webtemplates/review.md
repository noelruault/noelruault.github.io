# Review — Webtemplates

One line per group, appended by the builder that reviewed it inside the same cycle it built.

- reviewed t00-harness fcc83ed: REPAIRED — compare.mjs silently screenshotted a 404 page for a missing/unbuilt slug instead of failing; added an existsSync guard. The gate's `[class*="reveal" i]` selector convention was undocumented, meaning a future template that names its reveal wrapper differently would silently no-op every reveal/reduced-motion/no-js check; documented it in spec.md. Both fixed in 60397d7. No side-by-side parity verdict yet (no live template exists to compare — that starts at t10).
- reviewed t10-fin-ai 1729f6d: REPAIRED (8314ef0) — clean-room copy leakage: 128 shared 5-word shingles with the scraped source (all three testimonial quotes carried verbatim tails, ~14 headings kept the source predicate, footer/nav taxonomy copied word-for-word). Rewrote every hit; shingle scan now reports 0. Also verified this cycle: zero external requests at runtime (playwright request counter), no source class names in shipped files, gate mutation test (2200px div) exits 1, gate green after repair.
  - parity verdict t10-fin-ai @390/768/1440 (composites in .backups/webtemplates-verify/fin-ai/, live fetch OK): section order matches end to end (hero → 22 numbered reasons with 3 interleaved testimonials → event marquee → pricing → footer-CTA reprise → 5-col footer); tinted band positions match (warm 03, peach 08, pale blue 17); 1440 grid geometry matches (serif number rail / 6-col text / 3-col mono-labelled annotation rail / 8-col visual panel); 390 stacks single-column in the same order; motion present (pinned-hero curtain, reveal-on-scroll, marquee). Known gaps: live page is denser (real media and longer copy), mobile sticky section-jump subnav not built.
  - effects inventory: fixed announcement bar ✓ (JS close) · fixed nav + hover dropdowns ✓ (CSS) · pinned hero curtain ✓ (fixed + spacer) · 12-col reason grid ✓ · reveal-on-scroll ✓ (IO + no-IO + reduced-motion + noscript guards) · --i stagger ✓ (transform only) · tinted full-bleed themes ✓ · area chart ✓ (own SVG) · stat bar cards ✓ (CSS) · media panels ✓ (gradient + play pill) · testimonial figures with mark highlight ✓ · invented logo bands ✓ · event marquee ✓ (CSS keyframes, reduced-motion pause) · corner notch marks ✓ · footer-CTA reprise ✓ · burger panel ✓ · mobile section-jump subnav ✗ (recorded deviation).
- reviewed t05-modular-render 16b6b67: CLEAN — `renderSlug` is a genuine pure read (config.json + dynamic imports of shell.mjs/components), build.mjs's `existsSync(config.json)` guard means fin-ai (still hand-authored) takes zero code path changes, confirmed by rerunning the gate before and after (identical `ok fin-ai @ …` lines, no "rendered fin-ai/index.html"). Drift check verified live, not just via the fixture: added a throwaway `_stub` config-driven slug to manifest, gate green; hand-appended a comment to its committed `index.html` (no config change), gate printed `FAIL _stub: render-drift …` and exited 1; reverted the stub dir and manifest entry, gate green again. `rendertest.mjs` covers the same two branches (clean commit round-trips byte-identical, hand-edit diverges) permanently in an os-tmpdir fixture, wired into gate.mjs ahead of the manifest read so it can't pass silently. No regressions, no destructive ops, nothing else touched.
- reviewed t11-fin-ai-modular 0f1a647: REPAIRED — during self-review, hand-edit-mutation-tested the render-drift check against fin-ai specifically (confirmed FAIL on a real edit, then restored to green); this caught nothing new but is the assert-based proof the ticket's acceptance criterion demands. Two defects found and fixed before this line was written (see handoff.md): (1) the "steps" layout unconditionally used the bg-gradient article wrapper, rendering `class="undefined"` on r18, which has no background; (2) r09's "Queue&Co" picked up a stray trailing period versus the source during transcription. Screenshot-verified pixel parity against the pre-retrofit render at 390px/1440px across 4 scroll depths, normal + reduced-motion. Config now carries all copy/nav/stats/testimonials/footer links/gradient stops; SVG chart geometry (paths, tick coordinates) stays static in-component as decorative art, noted as a deviation, not a gap against the acceptance criterion (which only requires config-driven copy and pixel parity).
