# Handoff — Webtemplates

### t00-harness (fcc83ed)
meant: spec.md:37-49 "Harness contract" — bun workspace, manifest-driven build/gate, `bun run compare`.
did: `templates/scripts/check.mjs` (exported check fns + `checkSlugAtWidth`), `gate.mjs` (selftest → build → per-manifest-slug checks at 390/768/1440), `build.mjs` (tailwind CLI per manifest slug), `compare.mjs` (local-vs-live screenshot composites into `.backups/webtemplates-verify/<slug>/`), `selftest.mjs` (assert-based, catches its own mutated fixtures).
deviations: no `_stub` template kept in the tree — proved the harness live this cycle (stub in manifest, gate green; forced a real overflow into the stub, gate exit 1; reverted) then deleted the stub since `selftest.mjs` already carries that same proof permanently via inline HTML fixtures, so a committed stub dir would just be unused drift. `manifest.json` ships empty (no real template yet). `compare.mjs`'s "composite" is a single PNG (local|live side by side) rendered by loading both screenshots into one headless page and re-screenshotting it, not a pixel-diff tool — spec asked for a visual-parity aid, not a diff algorithm.
verified: `cd templates && bun install && bun run gate` → exit 0 (manifest empty, selftest runs and passes). Also ran gate with `_stub` temporarily in manifest: green on the clean stub, then red (`horizontal overflow`) after injecting a real overflow div, then green again after revert — this is the mutation proof the ticket asked for, done live rather than left as a persisted fixture.
threads: reveal-target detection convention is `[class*="reveal" i]` (case-insensitive substring match on class attribute) — not yet written into spec.md; whoever builds t10 should name reveal classes containing "reveal" (e.g. `fa-reveal`) or the gate's reveal/reduced-motion/no-js checks silently no-op (empty NodeList, always "pass"). Chromium browser binary was missing from this sandbox's playwright cache on first run (`bunx playwright install chromium`, took a few minutes over network) — already installed now, future cycles on this box should have it cached.
