# Handoff — Webtemplates

### t00-harness (fcc83ed)
meant: spec.md:37-49 "Harness contract" — bun workspace, manifest-driven build/gate, `bun run compare`.
did: `templates/scripts/check.mjs` (exported check fns + `checkSlugAtWidth`), `gate.mjs` (selftest → build → per-manifest-slug checks at 390/768/1440), `build.mjs` (tailwind CLI per manifest slug), `compare.mjs` (local-vs-live screenshot composites into `.backups/webtemplates-verify/<slug>/`), `selftest.mjs` (assert-based, catches its own mutated fixtures).
deviations: no `_stub` template kept in the tree — proved the harness live this cycle (stub in manifest, gate green; forced a real overflow into the stub, gate exit 1; reverted) then deleted the stub since `selftest.mjs` already carries that same proof permanently via inline HTML fixtures, so a committed stub dir would just be unused drift. `manifest.json` ships empty (no real template yet). `compare.mjs`'s "composite" is a single PNG (local|live side by side) rendered by loading both screenshots into one headless page and re-screenshotting it, not a pixel-diff tool — spec asked for a visual-parity aid, not a diff algorithm.
verified: `cd templates && bun install && bun run gate` → exit 0 (manifest empty, selftest runs and passes). Also ran gate with `_stub` temporarily in manifest: green on the clean stub, then red (`horizontal overflow`) after injecting a real overflow div, then green again after revert — this is the mutation proof the ticket asked for, done live rather than left as a persisted fixture.
threads: reveal-target detection convention is `[class*="reveal" i]` (case-insensitive substring match on class attribute) — not yet written into spec.md; whoever builds t10 should name reveal classes containing "reveal" (e.g. `fa-reveal`) or the gate's reveal/reduced-motion/no-js checks silently no-op (empty NodeList, always "pass"). Chromium browser binary was missing from this sandbox's playwright cache on first run (`bunx playwright install chromium`, took a few minutes over network) — already installed now, future cycles on this box should have it cached.

### t10-fin-ai (1729f6d)
meant: spec.md:18 clean-room rules + spec.md:27 method, applied to https://fin.ai/ into `templates/fin-ai/` with prefix `fa-`.
did: `templates/fin-ai/index.html` (header/announcement, pinned `.fa-hero` curtain, intro, white card with 22 `article.fa-reason`-shaped sections r01–r22 + 3 testimonial figures + `.fa-marquee-track` carousel, footer CTA reprise, footer); `src/components.css` (type scale, reveal, marquee, notch marks, hero pinning, dropdowns); `main.js` (header height var, announcement close, burger, IO reveal with fallback). Invented brand "Vela", models "Meridian", event "Beacon".
deviations: mobile-only sticky section-jump subnav ("01 Performance ▾") not built; nav dropdown contents are generic in-page anchors, not mega-menus; source's scroll-triggered video playback approximated as fade-up reveals; all photos/videos/3D renders replaced per clean-room rules (gradient panels, own knot/chart/flywheel SVGs); numeric claims re-invented (74%, 9,000+, 218 updates).
verified: `cd templates && bun install && bun run gate` green at 390/768/1440 (selftest, build, console/pageerror, overflow, reveal, reduced-motion, no-js, forbidden-identity); local screenshots eyeballed at 1440 and 390 against scraped reference shots.
threads: evidence scraped to /tmp/wt-finai (page.html, 5 css chunks, outline.json, ref screenshots), regenerate per-cycle, never committed; hero is position:fixed + `.fa-hero-spacer`, header height propagated via `--fa-head-h` set by main.js; gate's reveal check tolerates only ~300ms settle, so `.fa-reveal` opacity transition is 250ms with stagger applied to transform only.

### t05-modular-render (16b6b67)
meant: spec.md:30-36 "Modularity contract" — config.json + components/*.mjs + shell.mjs, build-time render, gate's render-drift check.
did: `templates/scripts/render.mjs` (`renderSlug`: reads config.json, imports shell.mjs + each `config.sections` component, joins); `build.mjs` calls it per manifest slug only when `<slug>/config.json` exists, writes `index.html` before the tailwind step; `gate.mjs` snapshots each modular slug's `index.html` before `build.mjs` runs, diffs after, exits 1 on any difference; `rendertest.mjs` (assert-based, tmp-dir fixture) proves a clean render is byte-identical and a hand-edit is caught, wired into gate.mjs the same way `selftest.mjs` is.
deviations: no committed stub template (same call t00 made) — proved the pipeline live with a throwaway `_stub` slug added to `manifest.json` (config-driven, gate green; hand-edited its `index.html`, gate exited 1; reverted both the dir and the manifest entry) instead of leaving unused fixture in the tree; `rendertest.mjs` fixture is the permanent proof, in an os-tmpdir dir, not under `templates/`. fin-ai is untouched (no config.json yet) — its retrofit is t11.
verified: `cd templates && bun run gate` green (selftest, rendertest, fin-ai css build + checks @390/768/1440) before AND after the spec.md edit. Live mutation proof: `_stub` in manifest → gate green; appended a comment line to `_stub/index.html` by hand (config unchanged) → gate printed `FAIL _stub: render-drift …` and exited 1; reverted → green again.
threads: `shell.mjs` convention (doctype/head/body + config-driven title/meta, joins `config.sections` in order) wasn't in spec.md before this cycle — added it to the Modularity contract so t11/t20+ know where the document wrapper lives; no real template uses it yet, first real usage is t11's fin-ai retrofit.
